#include "main.h"
/**
 * _isupper -prog checks for upper character
 * @c: variable task
 * Return: Always 0.
 */
int _isupper(int c)
{
if (c >= 'A' && C <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

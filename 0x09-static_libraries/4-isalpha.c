#include "main.h"
/**
 *_isalpha - check for alphabets
 *Description: we are using _putchar
 *@c: collect the character
 *Return: 1 if  c is lpw or cap otherwise 0
 */
int _isalpha(int c)
{
char lower, upper;
for (lower = 'a'; lower <= 'z'; lower++)
{
for (upper = 'A'; upper <= 'Z'; upper++)
{
if (lower == 'c' || upper 'C')
{
letter = 1;
}
}
}
return (letter);
}
